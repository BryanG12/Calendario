import { types } from "../types/types";


const initialState = {

}


export const calendarReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
}